# /docker-compose.yml
version: '3.8'

services:
  backend:
    build: ./backend
    restart: always
    expose:
      - "8000"
    environment:
      - SECRET_KEY=9a4f8b2c1d3e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0a
      - ACCESS_TOKEN_EXPIRE_MINUTES=60
    volumes:
      - ./backend:/app

  frontend:
    build: ./frontend
    restart: always
    expose:
      - "8501"
    volumes:
      - ./frontend:/app

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - backend
      - frontend

volumes:
  caddy_data:
  caddy_config: