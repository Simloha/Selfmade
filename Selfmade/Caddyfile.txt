# /Caddyfile
# Replace 'yourdomain.com' with your actual server domain. 
# Caddy automatically provisions and scales free SSL/TLS certificates via Let's Encrypt.

yourdomain.com {
    reverse_proxy /token backend:8000
    reverse_proxy /api/* backend:8000
    reverse_proxy * frontend:8501
}
